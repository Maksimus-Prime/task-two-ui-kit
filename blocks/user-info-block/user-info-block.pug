mixin user-info-block(photo,name,job,theme)
	.user-info-block.clearfix
		include ../photo-block/photo-block.pug
		include ../info-block/info-block.pug
		+photo-block(photo)
		+info-block(name,job,theme)
