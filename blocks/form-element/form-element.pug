mixin form-element(type,name,text)
	if type==="textarea"
		textarea.form-element__textarea(type="textarea" name=name+"" placeholder=text)
	else
		input.form-element__input(type=type+"" name=name+"" placeholder=text)