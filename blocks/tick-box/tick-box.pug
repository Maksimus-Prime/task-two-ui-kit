mixin tick-box(name,state)
	.tick-box
		-var name = "checkbox-"+name;
		-var state = state || false;
		input(type="checkbox" id=name+"" name=name+"" checked=("active"===state ? "checked" : undefined))
		label(for=name)