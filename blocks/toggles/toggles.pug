mixin toggles(name,state)
    label.toggles
        -var name = "checkbox-"+name;
        -var state = state || false;
        input.toggles__input(type="checkbox" id=name+"" name=name+"" checked=("active"===state ? "checked" : undefined))
        div.toggles__slider