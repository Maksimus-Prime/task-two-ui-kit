include ../../blocks/button/button.pug
include ../../blocks/form-line/form-line.pug
include ../../blocks/page-header/page-header.pug
include ../../blocks/page-footer/page-footer.pug
-var data = require('./data.json');
-var formData = data.loginFormData;
doctype html
html
    head
        meta(charset="UTF-8")
        meta(name="viewport" content="initial-scale=1, width=device-width")
        meta(name="description" content="one of the pages of meeting site")
        meta(name="keywords" content="login")
        meta(name="author" content="Lamonov Maks")
        title Login page
        link(rel="apple-touch-icon" sizes="180x180" href="favicons/apple-touch-icon.png")
        link(rel="icon" type="image/png" sizes="32x32" href="favicons/favicon-32x32.png")
        link(rel="icon" type="image/png" sizes="16x16" href="favicons/favicon-16x16.png")
        link(rel="manifest" href="favicons/manifest.json")
        link(rel="mask-icon" href="favicons/safari-pinned-tab.svg" color="#5bbad5")
        meta(name="theme-color" content="#ffffff")
        link(rel="stylesheet" href="main.css")
    body
        .login-page
            .login-page__header
                +page-header({title: "THE BEST MEETING WEBSITE", buttons: [{button: "goback", link: "main.html"}]})
            main.login-page__content
                p.login-page__note Please enter your phone or email. We'll send you a message with code
                form.login-page__main-form
                    .login-page__form-line
                        +form-line({inputtype: formData["email-field"].inputtype, name: formData["email-field"].name, placeholder: formData["email-field"].placeholder, valid: formData["email-field"].valid})
                    .login-page__form-line
                        +form-line({inputtype: formData["password-field"].inputtype, name: formData["password-field"].name, placeholder: formData["password-field"].placeholder, valid: formData["password-field"].valid})
                    .login-page__form-line
                        +button({text: "Log in", type: "button", link: "main.html", color: "green"})
            .login-page__footer
                +page-footer({text: "There is some information about this site"})
        script(type="text/javascript" src="main.js")